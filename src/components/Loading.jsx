const Loading = () => {
  return (
    <div className="flex justify-center bg-transparent min-h-screen place-items-center z-50">
      <button className="loading"></button>
    </div>
  );
};

export default Loading;
